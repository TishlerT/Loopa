package com.loopa.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Loopa corner radius tokens — ported from iOS TishRadius
 */
object TishRadius {
    /** 4dp — subtle rounding */
    val sm = 4.dp
    /** 8dp — standard buttons */
    val md = 8.dp
    /** 12dp — cards and containers */
    val lg = 12.dp
    /** 16dp — large cards */
    val xl = 16.dp
    /** Full circle */
    val full = 9999.dp
}
